<div class="share">
  <%=share.user.username%><%=share.write == true ? " (write)": ""%>
  <div class="share_edit_links">
    <%= link_to t(:edit), edit_share_path(share) %>
    <%= link_to(t(:delete),  share, :method => :delete, :confirm => "Are you sure?") %>
  </div>
</div>

